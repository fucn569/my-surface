#include <stdio.h>
int main()
{
    int a, r;
    for (a = 100; a <= 200;a++)
    {
        if(a%3==0)
            printf("%d\t",a);
        else
            ;
    }
    system("pause");
    return 0;
}
